syntax = "proto3"; // 协议版本
option java_outer_classname = "StudentProto"; // 生成的外部类，同时也是文件名
// protobuf 使用message 管理数据
message Student { // 会在StudentProto外部类生成一个内部类 Student，真正发送的POJO对象
    int32 id = 1; // Student 类中有一个属性 名字为id，类型为int32(proto类型) -> 1 表示属性编号，不是属性值
    string name = 2;
}